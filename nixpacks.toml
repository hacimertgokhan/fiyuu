[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["cd examples/fiyuu-website && npm install"]

[phases.build]
cmds = ["cd examples/fiyuu-website && npm run build"]

[start]
cmd = "cd examples/fiyuu-website && npm run start"
