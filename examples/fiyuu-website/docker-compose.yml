version: '3.8'

services:
  fiyuu-website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: fiyuu-website
    ports:
      - "4070:4070"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
