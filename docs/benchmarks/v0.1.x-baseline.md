# Fiyuu v0.1.x Baseline

Status: partial (SSR benchmark blocked by runtime error)

## Environment

- Date: 2026-03-28
- Machine: local development machine
- CPU: Apple M4 (10 cores)
- RAM: 24 GB
- OS: darwin 25.2.0
- Node: v25.8.0
- npm/pnpm/bun: npm

## Profile

- App profile: local starter app (`my-app`)
- Route count: 4
- Action count: TBD
- Query count: TBD

## Commands and Raw Outputs

### 1) Cold build

Command:

```bash
time npm run build
```

Raw output:

```text
0.46s (single run)
```

### 2) SSR latency + bundle size

Command:

```bash
npm run benchmark:gea
```

Raw output:

```text
createTinyInternalRouter is not defined
```

### 3) AI context readiness

Command:

```bash
time npx fiyuu sync
```

Raw output:

```text
0.27s (single run)
```

### 4) Doctor pass rate

Command:

```bash
npx fiyuu doctor
```

Raw output:

```text
healthy - no issues detected
```

## Summary Table

| Metric | Value |
| --- | --- |
| Build avg (s) | 0.46 (single run) |
| Build p95 (s) | TBD |
| SSR avg (ms) | blocked |
| SSR p95 (ms) | blocked |
| Bundle total (KB) | blocked |
| Sync time (s) | 0.27 (single run) |
| Doctor errors | 0 |
| Doctor warnings | 0 |

## Notes

- Keep this file immutable after release tags.
- If rerun is needed, add a new file with a clear suffix (example: `v0.1.x-baseline-r2.md`).
- Latest raw output snapshot: `docs/benchmarks/latest-scorecard.md`.
